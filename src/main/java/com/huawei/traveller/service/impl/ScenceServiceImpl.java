package com.huawei.traveller.service.impl;

import org.springframework.stereotype.Service;

import com.huawei.traveller.service.ScenceService;

@Service("scenceServiceBean")
public class ScenceServiceImpl implements ScenceService {
/*
	@Autowired
	public ScenceRepository scenceRepository;

	@Override
	public List<Scence> getScences() {
		return scenceRepository.findAll();
	}
	@Override
	public List<Scence> getScencesByPage(){
		return null;
	}
	@Override
	public Scence getScenceById(Integer id){
		return scenceRepository.findOne(id);
	}
	@Override
	public boolean updateScence(Scence scence){
		scenceRepository.save(scence);
		return true;
	}
	@Override
	public boolean saveScence(Scence scence){
		scenceRepository.save(scence);
		return true;
	}
	@Override
	public boolean deleteScenceById(Integer id) {
		scenceRepository.delete(id);
		return false;
	}

*/
}
